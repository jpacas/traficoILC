# Railway PostgreSQL connection URL
# Automatically provided by Railway when you add the PostgreSQL add-on
DATABASE_URL=postgresql://user:password@host:5432/database

# HTTP server port
# Railway will override this automatically
PORT=8080
